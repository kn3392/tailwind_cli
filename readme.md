###git initilization : git init
push to stage area : git add
commit : git commit -m"comit msg"
connrct to main branch : git branch -m main 
push the data to the git repo : git push origin main(branch_name )
push and update : git push -u origin main 
 create branch : git branch -m main 
 create branch : git branch branch_name , git checkout branch_name 
 switch branch : git switch branch_name 
 create and switch branch : git switch -c branch_name, git checkout -b branch_name 
 show the repo url : git remote -v
